/**
 * Rental Service — now just provides search utilities.
 * Actual data comes from the WAND API via wandService.
 */
const rentalService = {
  getAll() {
    return [];
  },

  getByRaNumber() {
    return null;
  },

  search() {
    return [];
  },
};

export default rentalService;
